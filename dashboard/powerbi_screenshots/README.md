# Power BI Screenshots

Save exported dashboard screenshots in this folder.

Expected screenshots:

- `executive_overview.png`
- `borough_operations.png`
- `agency_performance.png`
- `data_quality.png`

The `.pbix` file itself is ignored by Git because Power BI files can become large. Commit screenshots instead so reviewers can see the dashboard without opening Power BI.
